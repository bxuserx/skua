// tab-bar on top, terminal below. No status-bar: its hints describe zellij's
// own modal keys, not skua's Ctrl-a prefix (see config.kdl).
layout {
    pane size=1 borderless=true {
        plugin location="zellij:tab-bar"
    }
    pane
}
